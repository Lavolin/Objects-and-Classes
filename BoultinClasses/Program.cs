using System;

namespace BoultinClasses
{
    class Program
    {
        static void Main(string[] args)
        {
            Console.WriteLine(Math.Min(2, 3));
            Random random = new Random();
            random.Next();
        }
    }
}
